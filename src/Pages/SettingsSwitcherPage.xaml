<Page x:Class="Flarial.Launcher.Pages.SettingsSwitcherPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="SettingsSwitcherPage"
      Height="470"
      Width="555">
    <Page.Resources>
        <Style x:Key="PlatformTabStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="30"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Foreground" Value="#E7E7E7"/>
            <Setter Property="Background" Value="#221B1C"/>
            <Setter Property="BorderBrush" Value="#3A2F30"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="pack://application:,,,/Assets/#Space Grotesk"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#2F2526"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#3F2A2D"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#FF2438"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid HorizontalAlignment="Left">
            <Label Margin="0,0,0,5" HorizontalAlignment="Left" Content="Switcher" Foreground="White" FontSize="24" FontWeight="Bold" FontFamily="pack://application:,,,/Assets/#Space Grotesk"/>
            <Border Margin="5,0,0,5" VerticalAlignment="Bottom" Background="#FF2438" Height="3"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,6,0,8">
            <ToggleButton x:Name="AllTab" Style="{StaticResource PlatformTabStyle}" Content="All" IsChecked="True" Click="PlatformTab_Click"/>
            <ToggleButton x:Name="UwpTab" Style="{StaticResource PlatformTabStyle}" Content="UWP" Click="PlatformTab_Click"/>
            <ToggleButton x:Name="GdkTab" Style="{StaticResource PlatformTabStyle}" Content="GDK" Click="PlatformTab_Click"/>
        </StackPanel>

        <ListBox Grid.Row="2" x:Name="VersionsList" MouseDoubleClick="VersionsList_MouseDoubleClick" Background="#151515" Foreground="White" BorderBrush="#32292A" BorderThickness="1" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Label}" VerticalAlignment="Center" FontSize="15"/>
                        <Border Grid.Column="1" Background="#2A4A2F" CornerRadius="8" Padding="8,2" Margin="8,0,0,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsInstalled}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="Installed" FontSize="12"/>
                        </Border>
                        <Border Grid.Column="2" Background="#3A2F30" CornerRadius="8" Padding="8,2" Margin="8,0,0,0">
                            <TextBlock Text="{Binding Platform}" FontSize="12"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Grid.Row="3" x:Name="ProgressText" Text="Double-click a version to install" VerticalAlignment="Center" Foreground="#B0B0B0" Margin="0,10,0,0"/>
    </Grid>
</Page>
